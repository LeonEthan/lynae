{
  "name": "lynae",
  "private": true,
  "version": "0.0.1",
  "description": "Desktop AI coding workbench",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "turbo": "^2.4.4",
    "typescript": "~5.7.3"
  },
  "packageManager": "pnpm@10.5.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
